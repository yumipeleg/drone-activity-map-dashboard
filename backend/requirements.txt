# Runtime dependencies only. Keep this list minimal — add a package here
# only when the application actually needs it.

fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
psycopg[binary]>=3.2
pydantic>=2.0
pydantic-settings>=2.0
alembic>=1.13
